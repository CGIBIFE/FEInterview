import * as angular from 'angular';
import {TwitterFeedController} from './twitterfeed.controller';

export const TwitterFeedComponent: angular.IComponentOptions = {
    controller: TwitterFeedController,
    templateUrl: 'src/components/twitterfeed/twitterfeed.html'
};